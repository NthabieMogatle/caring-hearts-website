# Wire these up before launch — the contact form route currently only logs.

# Where new lead submissions should be delivered.
CONTACT_INBOX=hello@caringhearts.example

# Email provider key (Resend, SendGrid, Postmark, etc.) — choose one.
# RESEND_API_KEY=re_...
# SENDGRID_API_KEY=SG...

# Public site URL used in absolute links and OpenGraph.
NEXT_PUBLIC_SITE_URL=https://caringhearts.example
